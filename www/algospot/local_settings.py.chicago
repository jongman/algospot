# -*- coding: utf-8 -*-

import settings

settings.STATIC_ROOT = '/algospot/www_static'

settings.DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': 'algospot',
        'USER': 'algospot',
        'PASSWORD': '',
        'HOST': '',
        'PORT': '',
    }
}

