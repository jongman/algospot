# vim: set expandtab ts=2 sw=2 filetype=yaml:
---
- hosts: all
  vars:
    home_dir: /vagrant
    django_dir: /vagrant/www
    user: vagrant
  remote_user: vagrant
  tasks:
  - include: single_box.yml
